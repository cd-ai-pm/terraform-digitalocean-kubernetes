# tfsec configuration — aligns with terraform-aws-modules/* practice.
# https://aquasecurity.github.io/tfsec/latest/guides/configuration/config/

exclude_downloaded_modules: true

exclude_paths:
  - examples/**
  - .terraform/**
  - test/**

minimum_severity: MEDIUM
